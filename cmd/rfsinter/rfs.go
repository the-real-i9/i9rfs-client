package rfsinter

func Launch() {
	// connect to our websocket server with the existing authorization token from localStorage
	// if no auth token exists or it has expired, return an error saying that the user needs to login
}
